{
  "name": "todo-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express API for Todo webapp (in-memory store). Deps are installed at repo root; see /package.json.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  }
}
